import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// inside the HTML tag that has the id 'root', React JS inserts a component named App
ReactDOM.render(<App />, document.getElementById('root'));
